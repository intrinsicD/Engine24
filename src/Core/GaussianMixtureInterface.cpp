#include "GaussianMixtureInterface.h"

namespace Bcg {
    std::vector<float> GaussianMixtureInterface::pdf(const std::vector<Vector<float, 3> > &query_points) const {

    }

    std::vector<Vector<float, 3> > GaussianMixtureInterface::gradient(const std::vector<Vector<float, 3> > &query_points) const {

    }

    std::vector<Vector<float, 3> > GaussianMixtureInterface::normal(const std::vector<Vector<float, 3> > &query_points) const {

    }

    std::vector<Matrix<float, 3, 3> > GaussianMixtureInterface::hessian(const std::vector<Vector<float, 3> > &query_points) const {

    }
}